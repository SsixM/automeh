1. Не изменять `back/Trans.py`: сохранить его полностью как резервную рабочую версию.

2. Создать отдельный backend-файл, например `back/Trans_gemini.py`, на основе текущего `Trans.py`. Все изменения — только в этой копии:
   - убрать Whisper/faster-whisper импорт, инициализацию и вызов;
   - добавить Gemini transcription adapter с REST-запросами, моделью из `GEMINI_TRANSCRIBE_MODEL`, endpoint из `GEMINI_API_BASE_URL`, ключом из `GEMINI_API_KEY` и прокси из `PROXY_URL`;
   - сохранить существующий кэш транскрипта `.txt`, подготовку аудио и последующую генерацию/публикацию урока;
   - не добавлять fallback на Whisper, чтобы новый режим действительно использовал Gemini и явно сообщал о несовместимости API.

3. В `Trans_gemini.py` убрать только из нового пайплайна использование `processed_files`: не читать и не создавать `processed_files.json`, не вызывать его функции. Сам `back/processed_files.json` удалить по исходному запросу; основной `Trans.py` останется неизменённым, но при его последующем запуске он сможет создать базу заново — это будет явно указано в результате.

4. Очистить корневой `lessons.js`, оставив совместимый пустой массив `const lessons = [];`.

5. Убрать из нового файла hardcoded секретные fallback-значения: требовать переменные окружения и добавить безопасный `back/.env.example` без реальных значений (`GEMINI_API_KEY=`, `PROXY_URL=`, модель и endpoint). Переданные в сообщении секреты в файлы не записывать.

6. Выполнить локальные проверки без внешнего Gemini: Python-синтаксис нового файла, поиск остаточных вызовов Whisper/processed_files в новом пайплайне, проверка содержимого `lessons.js`, и убедиться, что оригинальный `Trans.py` не изменился. Ротацию уже раскрытых ключей рекомендовать отдельно.