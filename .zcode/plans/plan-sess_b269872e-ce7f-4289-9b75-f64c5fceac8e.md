1. Исправить конфигурацию и API-вызовы в `back/Trans_gemini.py`:
   - нормализовать `API_KEY`, чтобы значения вида `Bearer ...` не превращались в `Bearer Bearer ...` и не давали 401;
   - сделать пул текстовых моделей настраиваемым через `.env`, с рекомендуемым основным вариантом `claude-sonnet-5` и запасными моделями; не менять модель вслепую, потому что текущая проблема качества вызвана прежде всего входным транскриптом и промптом;
   - добавить явные таймауты/проверки и диагностические сообщения для пустого, подозрительно короткого или явно повреждённого транскрипта.

2. Переработать промпты генерации:
   - в системных правилах потребовать конспект, пригодный студенту: сохранить учебные факты, определения, объяснения преподавателя, примеры, алгоритмы, практические шаги, оговорки и домашнее задание; организационный шум отделять и сильно сокращать, но не удалять учебные указания;
   - запретить домысливание: неизвестное помечать как «неразборчиво/не подтверждено», не подменять лекцию общими знаниями;
   - перед генерацией требовать внутреннюю карту покрытия транскрипта по временным/тематическим сегментам, а в итоговом markdown — последовательное отражение всех содержательных сегментов; добавить блок «Что было на занятии»/«Итоги» только на основании источника;
   - передавать метаданным не только первые 3500 символов, а начало, середину и конец транскрипта, чтобы тема не определялась по вступлению;
   - убрать искусственную лаконичность для полного конспекта и задать ориентир полноты (не фиксированное число слов): примерно 1 полезный структурированный блок на каждый значимый фрагмент лекции;
   - для `content_tiny` сохранять определения, алгоритмы, числа, ограничения и практические действия, а не просто механически сокращать;
   - для quiz использовать весь конспект или тематические выдержки, распределять вопросы по темам и не допускать, чтобы все вопросы были только об административных правилах; добавить программную валидацию JSON, диапазона `correct`, числа вариантов, уникальности вопросов и покрытия тем.

3. Добавить постгенерационные проверки качества перед публикацией:
   - проверять, что `content` не подозрительно короткий относительно транскрипта, содержит несколько тематических разделов и не состоит только из организационных/ТБ-пунктов;
   - проверять согласованность `content_tiny` и quiz с полным конспектом;
   - при провале проверки не публиковать урок, сохранять step-файлы и писать понятную причину в лог, чтобы можно было исправить промпт/перегенерировать.

4. Сделать обработку транскрипции надёжнее:
   - не отправлять большие аудиофайлы как потенциально неподъёмный inline base64 без контроля размера; добавить безопасную проверку размера и понятную ошибку/альтернативный путь;
   - сохранить транскрипт и промежуточные шаги до успешной публикации;
   - после публикации не удалять папку автоматически по умолчанию (ввести `DELETE_AFTER_PUBLISH=false`), чтобы повторная генерация была возможна; удаление оставить явной настройкой.

5. Безопасно подготовить повторный запуск:
   - удалить из `lessons.js` только запись `informatika-texnika-bezopasnosti-2026-09-03`, не затрагивая остальные уроки;
   - очистить соответствующие кэш/processed-артефакты и оставить аудио для повторной транскрибации; отдельно проверить, какие каталоги реально относятся к этому уроку, прежде чем удалять;
   - не удалять другие ожидающие уроки в `back/vid`.

6. Проверить результат статическими проверками и тестовым запуском без публикации: синтаксис Python, корректность JSON/markdown, выбор модели, обработку ключа, пороги качества и сохранение кэша. Затем показать изменённые файлы и команды для безопасной генерации нового урока.